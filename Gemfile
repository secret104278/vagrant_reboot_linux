source 'https://rubygems.org'

group :development do
  gem 'rubocop', require: false
  gem 'vagrant', git: 'https://github.com/hashicorp/vagrant.git'
end

group :plugins do
  gem 'vagrant_reboot_linux', path: '.'
end
